name: familiars_server
description: Agent control surface — Phase 0 (Bones)
version: 0.0.1
publish_to: 'none'

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  shelf: ^1.4.0
  shelf_router: ^1.1.0
  drift: ^2.20.0
  sqlite3: ^2.4.0
  path: ^1.9.0

dev_dependencies:
  drift_dev: ^2.20.0
  build_runner: ^2.4.0
  lints: ^4.0.0
  test: ^1.25.0
